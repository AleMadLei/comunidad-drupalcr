/**
 * @file
 * DrupalCR behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.drupalcr = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
